# Changelog

All notable changes to this project will be documented in this file.

Format follows [Keep a Changelog](https://keepachangelog.com/), and this project
adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added

- Raycast extension (`raycast/`): browse focuses, view per-project git status, and
  run any `pos` command from Raycast over the CLI's JSON contract. MIT-licensed.
- Agent-first CLI output: machine-readable JSON when stdout isn't a TTY, plus
  `--json` help.
- `pos` sidecar names the new tab after the current folder.

### Changed

### Fixed
