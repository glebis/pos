## Summary

## Changes

## Test plan

- [ ] Tests pass (`uv run pytest -q`)
- [ ] Raycast tests pass, if `raycast/` touched (`cd raycast && npm test`)
- [ ] Docs updated, if behavior changed
- [ ] Changelog updated, if user-visible
